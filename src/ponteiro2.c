#include <stdio.h>
#include <stdlib.h>
#include "funcoes1.h"
// com o void ele é sem retorno (return), mas no int tem return 

//o print e uma função que esta na bliioteca include stdio = que e exibir haerd   ( entao pode ser criado qualquer biblioteca)
//exibir
int main(){
    system("clear");
    exibir(50);//aqui tem que ser número por conta que e int 
    return 0; 
}
